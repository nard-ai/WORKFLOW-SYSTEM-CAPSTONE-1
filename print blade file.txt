<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Request #{{ $formRequest->form_id }} - Print View</title>
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.bunny.net">
    <link href="https://fonts.bunny.net/css?family=figtree:400,500,600&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@600&display=swap" rel="stylesheet">

    <!-- Styles -->
    <style>
        @media print {
            @page {
                margin: 1cm;
            }
            body {
                font-family: 'Figtree', sans-serif;
                line-height: 1.5;
                margin: 0;
                padding: 20px;
            }
            .no-print {
                display: none !important;
            }
            .page-break {
                page-break-before: always;
            }
        }
        body {
            font-family: 'Figtree', sans-serif;
            line-height: 1.5;
            margin: 0;
            padding: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #000;
            padding-bottom: 20px;
        }
        .logo {
            max-width: 150px;
            margin-bottom: 10px;
        }
        .request-details {
            margin-bottom: 30px;
        }
        .section {
            margin-bottom: 20px;
        }
        .section-title {
            font-weight: 600;
            font-size: 1.1em;
            margin-bottom: 10px;
            border-bottom: 1px solid #ddd;
            padding-bottom: 5px;
        }
        .grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
            margin-bottom: 15px;
        }
        .field {
            margin-bottom: 10px;
        }
        .field-label {
            font-weight: 500;
            color: #666;
        }
        .field-value {
            margin-top: 2px;
        }
        .approval-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
            margin-top: 15px;
        }
        .approval-box {
            border: 1px solid #ddd;
            padding: 15px;
            text-align: center;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .signature-text {
            font-family: 'Dancing Script', cursive;
            font-size: 1.5em;
            margin-bottom: 5px;
            color: #000;
        }
        .approval-name {
            font-weight: 600;
            font-size: 1em;
            margin-bottom: 2px;
        }
        .approval-position {
            color: #666;
            font-size: 0.9em;
            margin-bottom: 2px;
        }
        .approval-department {
            color: #666;
            font-size: 0.9em;
            margin-bottom: 5px;
        }
        .approval-date {
            font-size: 0.8em;
            color: #666;
        }
        .approval-status {
            margin-top: 5px;
            font-size: 0.8em;
            padding: 2px 8px;
            border-radius: 12px;
            display: inline-block;
        }
        .status-approved {
            background-color: #dcfce7;
            color: #166534;
        }
        .status-noted {
            background-color: #dbeafe;
            color: #1e40af;
        }
        .no-print {
            position: fixed;
            top: 20px;
            right: 20px;
        }
        .print-button {
            background-color: #3b82f6;
            color: white;
            padding: 8px 16px;
            border-radius: 4px;
            text-decoration: none;
            cursor: pointer;
        }
        .print-button:hover {
            background-color: #2563eb;
        }
        @media print {
            @page {
                size: short;
                margin: 1.5cm;
            }
            body {
                font-size: 12px;
            }
            .section {
                margin-bottom: 15px;
            }
            .approval-grid {
                page-break-inside: avoid;
            }
            .header {
                margin-bottom: 20px;
            }
        }
    </style>
</head>
<body>
    <!-- Print Button -->
    <div class="no-print">
        <button onclick="window.print()" class="print-button">
            Print Request
        </button>
    </div>

    <!-- Header -->
    <div class="header">
        <h1>Request Details - #{{ $formRequest->form_id }}</h1>
        <p>Generated on {{ now()->format('F j, Y g:i A') }}</p>
    </div>

    <!-- Request Information -->
    <div class="section">
        <div class="section-title">Request Information</div>
        <div class="grid">
            <div class="field">
                <div class="field-label">Request Type</div>
                <div class="field-value">{{ $formRequest->form_type }}</div>
            </div>
            <div class="field">
                <div class="field-label">Status</div>
                <div class="field-value">{{ $formRequest->status }}</div>
            </div>
            <div class="field">
                <div class="field-label">Date Submitted</div>
                <div class="field-value">{{ $formRequest->date_submitted->format('F j, Y g:i A') }}</div>
            </div>
            <div class="field">
                <div class="field-label">Requester</div>
                <div class="field-value">{{ $formRequest->requester->username }} ({{ $formRequest->requester->department->dept_name }})</div>
            </div>
        </div>
    </div>

    <!-- Request Details -->
    <div class="section">
        <div class="section-title">Request Details</div>
        @if($formRequest->form_type === 'IOM')
            <div class="grid">
                <div class="field">
                    <div class="field-label">From Department</div>
                    <div class="field-value">{{ $formRequest->fromDepartment->dept_name }}</div>
                </div>
                <div class="field">
                    <div class="field-label">To Department</div>
                    <div class="field-value">{{ $formRequest->toDepartment->dept_name }}</div>
                </div>
                <div class="field">
                    <div class="field-label">Re (Subject)</div>
                    <div class="field-value">{{ $formRequest->title }}</div>
                </div>
                <div class="field">
                    <div class="field-label">Priority</div>
                    <div class="field-value">{{ $formRequest->iomDetails->priority ?? 'N/A' }}</div>
                </div>
                <div class="field">
                    <div class="field-label">Purpose</div>
                    <div class="field-value">{{ $formRequest->iomDetails->purpose ?? 'N/A' }}</div>
                </div>
                <div class="field">
                    <div class="field-label">Date Needed</div>
                    <div class="field-value">{{ $formRequest->iomDetails->date_needed ? \Carbon\Carbon::parse($formRequest->iomDetails->date_needed)->format('F j, Y') : 'N/A' }}</div>
                </div>
            </div>
            @if($formRequest->iomDetails)
                <div class="field" style="margin-top: 15px;">
                    <div class="field-label">Description/Body</div>
                    <div class="field-value" style="white-space: pre-wrap;">{{ $formRequest->iomDetails->body }}</div>
                </div>
            @endif
        @elseif($formRequest->form_type === 'Leave')
            <div class="grid">
                <div class="field">
                    <div class="field-label">Leave Type</div>
                    <div class="field-value">{{ $formRequest->leaveDetails->leave_type ?? 'N/A' }}</div>
                </div>
                <div class="field">
                    <div class="field-label">Start Date</div>
                    <div class="field-value">{{ $formRequest->leaveDetails ? \Carbon\Carbon::parse($formRequest->leaveDetails->start_date)->format('F j, Y') : 'N/A' }}</div>
                </div>
                <div class="field">
                    <div class="field-label">End Date</div>
                    <div class="field-value">{{ $formRequest->leaveDetails ? \Carbon\Carbon::parse($formRequest->leaveDetails->end_date)->format('F j, Y') : 'N/A' }}</div>
                </div>
                <div class="field">
                    <div class="field-label">Reason</div>
                    <div class="field-value">{{ $formRequest->leaveDetails->reason ?? 'N/A' }}</div>
                </div>
            </div>
        @endif
    </div>

    <!-- Approval Timeline -->
    <div class="section">
        <div class="section-title">Approval Timeline</div>
        <div class="approval-grid">
            @foreach($formRequest->approvals->sortBy('action_date') as $approval)
                @if($approval->action !== 'Submitted')
                    <div class="approval-box">
                        @if($approval->signature_data)
                            <img src="{{ $approval->signature_data }}" alt="Digital Signature" style="height: 40px; margin-bottom: 5px;">
                        @else
                            <div class="signature-text">
                                {{ $approval->approver->employeeInfo->FirstName }} {{ $approval->approver->employeeInfo->LastName }}
                            </div>
                        @endif
                        <div class="approval-name">
                            {{ $approval->approver->employeeInfo->FirstName }} {{ $approval->approver->employeeInfo->LastName }}
                        </div>
                        <div class="approval-position">
                            {{ $approval->approver->position }}
                        </div>
                        <div class="approval-department">
                            {{ $approval->approver->department->dept_name }}
                        </div>
                        <div class="approval-date">
                            {{ \Carbon\Carbon::parse($approval->action_date)->format('M j, Y g:i A') }}
                        </div>
                        <div class="approval-status {{ $approval->action === 'Approved' ? 'status-approved' : 'status-noted' }}">
                            {{ $approval->action }}
                        </div>
                    </div>
                @endif
            @endforeach
        </div>
    </div>

    <script>
        // Auto-print when the page loads
        window.onload = function() {
            // Add a small delay to ensure everything is loaded
            setTimeout(function() {
                window.print();
            }, 500);
        };
    </script>
</body>
</html> 